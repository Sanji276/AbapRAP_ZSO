projection;
strict ( 2 );
use draft;
define behavior for YC_OCRD alias cardcode
{
  use create;
  use update;
  use delete;

  use action setActive;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}